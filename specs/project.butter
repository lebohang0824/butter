# Butter Project — Release Workflow Specification
#
# This specification defines the sequential release steps for the Butter project.
# AI agents: read this spec to execute release tasks safely and deterministically.
# Follow each action in sequence, ensuring that any constraints
# are verified against the repository state before executing.

app ButterProject
  description "Release workflow for the Butter project. AI agents: honor all instructions, follow only the defined execution steps sequentially, and strictly respect all constraints and enforcement rules."

feature ReleaseFlow
  description "Sequential execution flow to track changes, run validation tests, bump version metadata, update changelogs and documentation, and push the release to the remote repository."
  version "2.1.0"

  actions
    # Step 1: Pre-release Inspection
    # AI agents: Inspect the git workspace first to see what files are modified.
    "Track changes"

    # Step 2: Quality Assurance & Validation
    # AI agents: Compile and validate all .butter specs to prevent broken configurations.
    "Test all butter spec files"

    # Step 3: Versioning & Changelog updates (compiler changes only)
    # AI agents: The following steps apply only when the changes involve compiler
    # changes. Skip them when only spec files or documentation changed.
    "Update version"
      enforce "Only when the changes involve compiler changes"
    "Update changelog to reflect new changes"
      enforce "Only when the changes involve compiler changes"
    "Update all documents (.md and docs/index.html)"
      enforce "Only when the changes involve compiler changes"
    "Update docs/extension.html"
      enforce "Only when the changes involve compiler changes"

    # Step 4: Finalizing & Publishing
    # AI agents: Grouped git operations to stage, verify, commit, and push changes to main.
    "Stage all changed files"
    "Verify git status shows only intended changes"
    "Commit with conventional message format 'type(scope): description'"
    "Push to main branch"
